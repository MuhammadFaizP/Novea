import { MD3LightTheme as DefaultTheme } from 'react-native-paper';

const theme = {
    ...DefaultTheme,
    colors: {
      ...DefaultTheme.colors,
      primary: "#0056b3",
      accent: "#ffffff", 
      background: "#f0f0f0",
      surface: "#ffffff",
      text: "#333333",
      disabled: "#bdbdbd",
    },
  };

export default theme;